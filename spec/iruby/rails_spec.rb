require 'spec_helper'

describe Iruby::Rails do
  it 'has a version number' do
    expect(Iruby::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
